const Home = require("./Home")


describe('Addition', () => {
    it('knows that 2 and 2 make 4', () => {
      expect(2 + 2).toBe(4);
    });
});

describe("Login",()=>{
    describe("When given username and password",()=>{
        it("should let the user know if they are allowed in or not",()=>{
            
        })
    })
})